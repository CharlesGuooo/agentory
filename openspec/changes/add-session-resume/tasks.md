> 1 与 2 是纯函数，严格 TDD（先写测试、看它红、再实现）。
> 3 是界面，无自动化测试 —— 靠冒烟 + 真机验收，理由见 design.md D-R5。

## 1. 恢复命令（纯函数）

- [x] 1.1 五个 agent 的命令已穷举测试，含 pi / grok 的两条否定性断言
      其中两条是**否定性断言**，防的是静默失败：
      pi 的命令**不得**含 `--session-id`、grok 的**不得**含 `--session-id`
- [x] 1.2 已测：`codex resume <id>`，且断言 args 里不出现任何 `--` 开头的 flag
      （`docs/research-notes.md §8` 的验收标准）
- [x] 1.3 已测：`cwdExists=false` 与 `cwd=null` 分别抛不同的错，且只抛错不返回结果
      且**不回退**到任何别的目录
- [x] 1.4 `src/main/sessions/resume.ts`，10 个测试

## 2. 搜索与筛选（纯函数）

- [x] 2.1 已测：文本匹配覆盖 cwd 与 nativeTitle，忽略大小写，cwd 为 null 不崩
- [x] 2.2 已测：agent 筛选、空数组视同不限制、筛选后仍倒序
- [x] 2.3 已测：无匹配返回空数组；全空白文本视同无条件
- [x] 2.4 `src/main/sessions/filter.ts`，11 个测试

## 3. 历史视图（界面）

- [x] 3.1 `sessions:list` 与 `sessions:resume`；后者复用抽出来的 `spawnManaged`，避免会话表分裂成两套
- [x] 3.2 侧栏「历史会话」入口显示真实条数，点击打开独立视图
- [x] 3.3 agent / 工作目录 / 时间 / 原生标题都已显示
- [x] 3.4 目录已消失的条目划删除线、`aria-disabled`、点击被拦下，但仍然列出
- [x] 3.5 时间不精确的条目带 `≈` 标记
- [x] 3.6 搜索框 + agent 筛选芯片
- [x] 3.7 点击 → 新标签页恢复。**顺带补上了多终端支持**（`Map<id, Pane>` + 切换显示），否则恢复会顶掉正在用的会话
- [x] 3.8 侧栏默认行为未改，437 条不进侧栏

## 4. 验证

- [x] 4.1 118 个测试 / 14 个文件通过，`tsc EXIT=0`
- [x] 4.2 冒烟通过
- [x] 4.3 **达成**：新开标题 `✳ Claude Code` vs 恢复标题 `✳ 查看Claude.md文件了解需求` —— 用窗口标题（OSC 0）作证据，比断言屏幕文字稳，且写成了常驻测试
      **看到之前的对话内容回来** —— 不是"进程起来了"就算过
- [x] 4.4 codex 已验：子命令形式，收到 837997 字节
- [x] 4.5 **已量**：435 行，搜索框每次按键整表重建 **0.4 – 8.8 ms**，远在 16 ms 帧预算内。结论是什么都不做，不引入虚拟滚动

## 5. 收尾

- [x] 5.1 `docs/research-notes.md §3.3` 的 grok 记载已修正（`--session-id` → `--resume`），并注明了那个参数「不存在就创建」的陷阱
- [x] 5.2 `ponytail-review` 已跑并应用。**最有价值的一条不是砍行数**：
      渲染层把 `filterSessions` 的规则**手抄了一遍** —— 主进程那份有 11 个测试守着，
      渲染层那份一个都没有，等于**让那些测试变得没意义**（被测的不是真正跑的）。
      改成直接 import。为此还得把 `makeSession` 从 `types.ts` 挪到 `scan.ts` ——
      否则渲染层 import 纯函数会被连带拉进 `node:fs`。
      已验证渲染层产物里 `node:fs` 出现 0 次。
      **未做**：把 67 行冒烟脚手架从主进程抽出去（已记为待办，价值低于风险）
