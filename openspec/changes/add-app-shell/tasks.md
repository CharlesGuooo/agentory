> **全部已完成。** 本变更是先实现、后补规格（原因见 proposal 顶部），
> 所以这份清单记录的是**已经做完并验证过的事**，不是待办。

## 1. 窗口外壳

- [x] 1.1 主进程改用 `titleBarStyle: "hidden"` + `titleBarOverlay`，藏掉系统标题栏
- [x] 1.2 顶栏高度提取为 `TITLEBAR_HEIGHT = 40`，CSS 侧对应 `--titlebar-h`，两处互相注明
- [x] 1.3 顶栏整条设为 `-webkit-app-region: drag`，其上的按钮用 `no-drag` 挖回来
- [x] 1.4 新增 `window:overlay` IPC，渲染层应用主题后回传 `chrome` / `dim` 两色，主进程据此调用 `setTitleBarOverlay`

## 2. 布局

- [x] 2.1 `index.html` 重构为 顶栏 / 侧栏 / 终端 / 设置弹窗 四块
- [x] 2.2 `style.css` 重写，**全部取色自 `--c-*`**，无硬编码色值（例外：关闭按钮悬停红，Windows 约定色）
- [x] 2.3 新增 `src/renderer/shell.ts`：标签页、侧栏树、主题卡片、设置弹窗的 DOM 渲染
- [x] 2.4 终端容器改为占据右侧区域并随窗口 fit；实测列数由 164 降到 135，确认侧栏真的占位

## 3. 工作集侧栏与标签页

- [x] 3.1 会话按**真实 cwd 的最后一段**分组，不解码 agent 的存储目录名
- [x] 3.2 三态圆点（工作中 / 等你输入 / 已停），取色自当前主题的 ANSI 色槽
- [x] 3.3 会话退出时状态更新为「已停」，底部在跑计数相应变化
- [x] 3.4 摘要位显示「（摘要功能还没做）」—— **不编造假摘要**（D-S3）

## 4. 设置弹窗

- [x] 4.1 齿轮打开、Esc / 点遮罩关闭、打开时焦点移入
- [x] 4.2 明暗分段控件（跟随系统 / 浅色 / 深色），与主题选择相互独立
- [x] 4.3 主题卡片网格，每张卡内是**微缩的应用截面**（侧栏 + 终端内容，含 diff 红绿、警告黄、路径青、光标）
- [x] 4.4 卡片随当前明暗显示对应的那一面；选中卡片有描边与勾选标记
- [x] 4.5 用户主题加载失败时在弹窗内显示具体原因，不静默丢弃
- [x] 4.6 点击卡片立即换肤（顶栏 / 侧栏 / 终端 / 原生窗口控件全部跟随）

## 5. 验证

- [x] 5.1 `tsc --noEmit` 通过
- [x] 5.2 33 个单元测试通过（外壳是 DOM 渲染，未新增单元测试 —— 见下方说明）
- [x] 5.3 命令行冒烟通过：`bridge:true`、`screenMounted:true`、主题 `graphite/dark/#16181D`、会话已起
- [x] 5.4 真机验收：用户确认外观定稿（「没问题了」）

> **测试覆盖的实情**：本变更没有新增自动化测试。
> 外壳是 DOM 渲染，用 vitest 测它需要引入 jsdom 与一套 DOM 断言设施 ——
> 而 `README.md §9` 明确列出「明确不要用的：`senior-qa` / `webapp-testing`，
> 会去搭一套用不上的前端测试设施」。
> 现阶段外壳的正确性靠**命令行冒烟 + 真机验收**保证。
> 等外壳里出现真正的逻辑（多会话的排序、分组、状态机），再单独把那部分抽成可测的纯函数。
