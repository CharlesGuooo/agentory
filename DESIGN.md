# DESIGN.md — 产品设计（活文档）

> 这份文档记录**已经拍板的设计决策**和**支撑它们的实测数据**。
> `README.md` 是上一轮的调研交接；本文档在它之上做修正，冲突时**以本文档为准**。
> 状态：进行中（brainstorming 阶段）。最后更新 2026-08-12。

---

## 0. 本轮修正了 README 的哪些判断

| README 原判断 | 修正 |
|---|---|
| §2「三条护城河：统一索引 / 统一恢复 / 摘要」 | 前两条已被 `jazzyalex/agent-sessions` 做完（macOS）。**剩下的差异化 = 跨 agent + 摘要**（见下方补充，「Windows」已不再是差异化） |

> **2026-08-12 补充修正（发现于命名调研）**：`doctly/switchboard`（326★、MIT、179 commits、活跃维护）
> 是一个 **Windows 有 .exe 的桌面应用**，做的正是：读 `~/.claude/projects`、跨项目统一列出会话、
> **内嵌完整终端**、launch / resume / fork / monitor、全文搜索 + 活动热力图。
>
> **它推翻了「Windows 上没有会话索引器」这个前提。** 但两个缺口正是本项目剩下的护城河：
> **① 只支持 Claude Code 一个 agent　② 不做 AI 摘要。**
>
> 因此差异化表述必须收紧为：**Windows 上唯一的跨 agent（Claude + Codex + OpenCode + Pi + Grok）
> 会话工作台，且唯一会告诉你每个会话在干什么的。**
> 附带的好消息：它验证了 D-1 的形态（内嵌终端 + 索引）在 Windows 上用 Electron 可行且已有人发布。
| §4.4「MVP 先做 CLI 不做 GUI」 | **推翻**。用户的核心痛点是「多个 agent 之间怎么切换」，这个痛点只有 GUI 能解 |
| §4.5「颜色统一做不到字面意义那样」 | **过度警告**。用户要的就是调色板映射，xterm.js 原生支持，是配置不是开发 |
| §6-2「OpenCode SQLite schema 没读过，是风险点」 | **已读，且它是五个里最简单的**（见 §2） |
| §6-1「技术栈未定，Electron vs Tauri」 | 仍未定，但已确定要内嵌终端（见 D-1） |
| §9「用 database-schema-designer 设计 launch ledger」 | **不需要**。全量扫描 483ms，不建索引库；工作集只有几十条，JSON 足够 |

---

## 1. 定位（修正后）

**Windows 上的跨 agent 会话工作台** —— 把散落在 5 个 PowerShell 窗口里的 agent 会话，收进一个带 explorer + tab 的窗口；关机后能一键恢复；每个会话有一句话说明它在干什么。

三个支柱，按不可替代性排：

1. **重启恢复**（用户第一痛点：关机后会话全丢）
2. **会话摘要**（全网无人做，唯一原创功能）
3. **跨 agent 统一切换**（explorer + tab，替代「一堆 PowerShell 窗口」）

---

## 2. 实测事实（2026-08-12，本机 `C:\Users\PC`）

**全五个 agent、847 个会话、全量扫描 + 真实 cwd 解析 = 483 ms。**

> ### ⚠ 2026-08-13 实测修正：Claude 的会话数是错的，847 这个总数也是错的
>
> 8-12 数 Claude 会话时用了 `-Recurse`，把**子 agent 的 transcript 和工具输出也算成了会话**。
> 真实结构是：
>
> ```
> ~/.claude/projects/<编码目录>/
>     <uuid>.jsonl          ← 真正的会话
>     <uuid>/               ← 该会话的附属目录，不是会话
>         subagents/agent-*.jsonl   ← 子 agent transcript，全部带 "isSidechain": true
>         tool-results/  workflows/
>     memory/
> ```
>
> **Claude 扫描器只取深度 2 的 `<编码目录>/*.jsonl`，不得递归。**
> 实测：深度 2 有 74 个文件，**无一带 `isSidechain`**；抽样 40 个子 agent 文件**全部带**。
>
> | Agent | 8-12 记录（错） | 8-13 实测（对） |
> |---|---|---|
> | claude | 487 | **74** |
> | codex | 65 | 65 |
> | opencode | 166 | 166 |
> | pi | 11 | 12 |
> | grok | 118 | 120 |
> | **合计** | **847** | **437** |
>
> ### 2026-08-13 索引层实现完成，五个 agent 全量实测
>
> ```
> 全量扫描：437 个会话，339 ms
>   claude      74    cwd  74/74     原生标题 0
>   codex       65    cwd  65/65     原生标题 0
>   opencode   166    cwd 166/166    原生标题 166
>   pi          12    cwd  12/12     原生标题 0
>   grok       120    cwd 120/120    原生标题 0
>   cwd 解析成功 437/437（100%）· cwd 已消失 35 · 时间不精确 0 · 问题记录 0
> ```
>
> - **`README.md §8` 的验收标准达成**：不是抽查，是全量断言 —— 每条记录要么 cwd 为 null
>   且 `cwdExists=false`，要么是绝对路径且 `cwdExists` 与真实存在性一致，没有第三种状态
> - **35 个会话的目录已被删除**（多为临时目录），全部保留并标记，不丢弃用户的历史
> - **1 秒上限与只读性都写成了常驻断言**，不是一次性验证。超时会让测试变红，
>   而不是被悄悄容忍然后加个缓存 —— 那条上限的意义就是守住 D-4
> - **摘要的成本再次下修**：原生标题只有 OpenCode 的 166 条，
>   需要调模型的是 **271 个**会话，不是 D-7 原估的 678 个
>
> **叙事也随之改变**：会话最多的是 **OpenCode（166）**，不是 Claude。
> Claude 是**最活跃**的那个（今天仍在用），但不是量最大的。
> 摘要的成本估算也要减半（原估 678 个待生成 → 约 271 个）。

> ### 2026-08-13 实测：`lastActivity` 不能用文件 mtime
>
> mtime 恒比内容里最后一条时间戳新，活跃会话差 0，但实测有差 **16.5h / 59.8h / 189.7h（8 天）** 的。
> 原因：Claude Code 会追加 `{"type":"last-prompt"}` 这类**不带 timestamp 的簿记记录**，
> 写入更新了 mtime 却没有新的对话内容。
>
> **mtime 反映「文件被写过」，内容时间戳才反映「用户真的在这儿工作过」。**
> 索引按最后活动排序，必须用后者。
> 代价实测：尾部读 16KB 提时间戳，74 个文件 4 ms —— 可以忽略。

| Agent | 会话数 | 体积 | 存储形态 | 真实 cwd 来源 | 原生标题 | 最后使用 |
|---|---|---|---|---|---|---|
| **claude** | 487 / 53 目录 | 1007 MB | 每会话一个 `.jsonl` | 扫前 40 行找 `cwd`，**486/487 命中，240ms** | 无 | 今天 |
| **opencode** | 166 / 32 目录 | 864 MB | **SQLite 单库** | `session.directory` 列，**直接可用** | **有，质量很好** | 07-17 |
| **grok** | 118 / 90 目录 | 77 MB | 项目目录 → **UUID 子目录**（会话是目录不是文件） | URL 解码目录名，**无损，118/118 全部存在** | 待测 | 08-05 |
| **codex** | 65 | 169 MB | 按日期分区 `YYYY/MM/DD/` | 扫前 40 行，**65/65 命中，92ms** | 待测 | 07-17 |
| **pi** | 11 / 8 目录 | 12 MB | 每会话一个 `.jsonl` | **第 1 行 header 直接有**，11/11 | 待测 | 08-11 |

### 2.1 OpenCode schema（README §6-2 空白已关闭）

`session` 表直接提供，**不需要任何解码或扫描**：

```
id, project_id, parent_id, slug, directory, title, version,
time_created, time_updated, time_archived,
agent, model, cost,
tokens_input, tokens_output, tokens_reasoning, tokens_cache_read, tokens_cache_write
```

其它相关表：`project(id, worktree, name, ...)`、`message(id, session_id, data)`、`part(id, message_id, data)`、`todo`。
读取方式：`sqlite3 "file:<path>?mode=ro"`，已验证可在有 WAL 的情况下只读打开。

### 2.2 sessionId 稳定性（决定工作集能不能存 id）

Claude 会话文件**最长时间跨度 1932 小时（80 天）**，22 个文件跨度 > 24 小时
→ **`claude --resume <id>` 是就地追加，sessionId 不变**。工作集里存 sessionId 是安全的。

> 待验证：codex / opencode / pi / grok 的 resume 是否同样就地追加。

### 2.3 首条用户消息作为摘要来源（决定摘要方案）

484/487（99.4%）能提取到首条真实用户消息（已排除 `<command-*>` / `<system-reminder>` / `Caveat:` 注入）。

| 长度 | 数量 | 占比 |
|---|---|---|
| < 15 字符（无信息） | 2 | 0.4% |
| 15–80（可直接用） | 22 | 4.5% |
| 80–400（需压缩） | 30 | 6.2% |
| **> 400（必须压缩）** | **430** | **88.8%** |

**关键发现：很多 prompt 是「角色设定在前、主题在后」的结构**
（例：`你是研究助理。请做中文互联网深度调研（今天是2026年7月30日），用 WebSearch...`）
→ **纯截断会正好切掉主题，必须过模型压缩。**

### 2.4 会话尾部的体积（决定摘要能不能直接发尾部）

40 个最近 Claude 会话样本：

| 指标 | 值 |
|---|---|
| 文件大小 | 平均 **10.7 MB**，最大 **195.8 MB** |
| 最后 20 行体积 | 平均 **63.6 KB**（≈16000 token），最大 **361 KB**（≈9 万 token） |
| 其中 `tool_result` / `tool_use` 行占比 | **56%** |
| 尾部 > 100 KB 的会话 | 4 / 40 |

**两个结论：**
1. **绝不能整文件读** —— 195 MB 的单会话文件存在。尾部必须用 FileStream 从末尾 seek
   （实测：`Get-Content -Tail` 在这些文件上会超时）。
2. **绝不能原样发尾部** —— 必须先过滤掉工具行。过滤同时解决三件事：
   成本、上下文爆掉、**以及隐私 —— 因为源码正藏在 `tool_result` 里**。

### 2.5 本机工具链（决定技术栈可行性）

| | 版本 |
|---|---|
| node / npm / pnpm | v24.13.0 / 11.7.0 / 10.33.0 |
| **cargo / rustc** | **1.93.0（已装）** |
| **Visual Studio Build Tools 2022** | **已装**（node-pty 编译原生模块的前提） |
| python | 3.14.2 |
| Windows build | **26200**（≥ 22621，支持 ConPTY PASSTHROUGH_MODE） |

**ConPTY passthrough 的分歧**：`PSEUDOCONSOLE_PASSTHROUGH_MODE`（VT 序列直通，
不经 ConPTY 屏幕缓冲重绘，保留光标形状 / DECSCUSR）——
`node-pty` **不暴露**（只有 `conptyInheritCursor`）；Rust 的 `portable-pty` **有**
（在 `portable-pty-psmux` fork 里）。这直接关系到 D-3「原生 TUI 保真」。

### 2.6 五个 agent 的 transcript 结构（README §6-3 空白已关闭）

| Agent | 消息文件 | 消息形状 | 真实 cwd | 原生摘要 |
|---|---|---|---|---|
| **claude** | `<uuid>.jsonl` | `{"type":"user"\|"assistant","message":{role,content[]}}` | 扫前 40 行的 `cwd` | 无 |
| **codex** | `rollout-*.jsonl` | `{"type":"response_item","payload":{"type":"message",role,content:[{input_text}]}}` | **`turn_context.payload.cwd`**（不在 `session_meta` 里） | 无 |
| **pi** | `<ts>_<uuid>.jsonl` | `{"type":"message","message":{role,content:[{type:"text",text}]}}` | **第 1 行 header 直接有** | 无 |
| **grok** | `<proj>/<uuid>/chat_history.jsonl` | `{"type":"user"\|"assistant","content":[{type:"text",text}]}` | **`summary.json` → `info.cwd`** | `session_summary` —— **不可用** |
| **opencode** | SQLite `message` / `part` 表的 `data` 列 | JSON | `session.directory` | **`title` —— 可用** |

**Grok 的会话目录**（一个会话 = 一个目录）还含：`events.jsonl`、`updates.jsonl`（ACP 协议流，
`user_message_chunk` / `agent_thought_chunk` / `tool_call`）、`system_prompt.txt`、`rewind_points.jsonl`、
`prompt_context.json`、`*.lock`。取消息用 `chat_history.jsonl` 最干净。
`summary.json` 还免费给了 `num_messages` / `created_at` / `updated_at` / `current_model_id`。

**Grok 的 `session_summary` 是反面证据，正好支持 D-7**：它就是首条消息的粗暴截断 ——
`"﻿Use brave web search to find out who won the"`（从句中切断，还带 BOM）、`"skills"`。
**证明纯截断不够用，必须过模型。**

#### ⚠ 五个 agent 通用的坑：首条「用户消息」通常是注入，不是用户真正打的字

| Agent | 需要过滤掉的注入 |
|---|---|
| claude | `<command-*>`、`<local-command-*>`、`<system-reminder>`、`Caveat:` 开头 |
| codex | `role == "developer"`（permissions instructions）、首个 user 消息里的 `# AGENTS.md instructions` |
| grok | `<user_info>`、`<system-reminder>` |
| pi | **无包装**（实测）。但有 `/help` 这类斜杠命令，是命令不是话题 |
| opencode | **无包装**（实测）。且 `session.title` 质量可用 |

摘要（D-7）取「首条 prompt」时**必须先过掉这些**，否则 847 个会话的摘要会全部变成同一段系统提示。
实测：加上这层过滤后，Claude 的提取成功率是 **484/487（99.4%）**。

---

## 3. 已定决策

### D-1 · 形态：内嵌终端的 working station
左侧 explorer + 顶部 tab，会话跑在应用内（xterm.js + ConPTY）。
**理由**：核心痛点是「多个 agent 之间切换」，不是「找到历史会话」。伴生式（只索引、在外部终端里拉起）解不了这个痛点。
**代价**：正面进入 Pane（AGPL、Windows 一等公民、日更）的地盘 → 由 D-2 约束。

### D-2 · 终端是买来的黑盒，不是造的模块
应用对终端只做三件事：**起 PTY / 把字节喂给 xterm.js / 设一个 theme 对象**。
终端层**不写任何自有代码**：不做复制粘贴增强、不做 scrollback 搜索、不做 URL 检测、不做分屏。
**理由**：那是 Pane 每天发数版在做的事；而且 agent 自己在快速演进输入体验（如 Claude Code 新增的长文本操作），在终端层加东西 = 未来和 agent 打架。

### D-3 · 主题 = 纯调色板映射，不碰字节流
只替换 xterm.js 的 `theme`：16 个 ANSI 槽 + background / foreground / cursor。
agent 的启动动画、排版、Pi 的极简风格 **一律不动**。浅色/深色模式 = 换一套这 19 个值。
> **2026-08-12 实测修正：原来写的 `COLORTERM` 旋钮不存在，此处已推翻。**
>
> 探针对 Claude 做了 A/B（设 `COLORTERM=truecolor` vs 完全不设），两次输出
> **逐字节完全相同**（各 15902 字节，全部信号一致，零个只在一侧出现的序列）。
> 五个 agent 全量实测的颜色用法：
>
> | agent | 真彩 `38;2;` | 256 色 `38;5;` | DECSCUSR | 备用屏幕 |
> |---|---|---|---|---|
> | claude | 76 | 0 | 0 | 1 |
> | codex | 5 | **21** | **3** | 0 |
> | opencode | 430 | 0 | **2** | 1 |
> | pi | 214 | 0 | 0 | 0 |
> | grok | **2315** | 0 | 0 | 1 |
>
> **结论一：五个 agent 都直接发 24 位 RGB，不看 `COLORTERM`。**
> 没有办法把它们"降级"到 16 色 —— 那个开关做不出来，从设计里删掉。
>
> **结论二：16 个 ANSI 色槽的实际影响远小于原先假设。**
> agent 的彩色输出绕过调色板。主题真正控制的是
> **背景 / 默认前景 / 光标 / 选中态 + 整个应用外壳** —— 也就是"地面"。
> 16 槽仍然有用（基础 SGR、codex 的 256 色、以及未来的第 6 个 agent），但不是主角。
>
> **结论三：这恰恰符合用户的原始要求。** 用户说的是「agent 自己的显示方式我们都不要变化，
> 只改背景和 App 的配色」—— agent 保留品牌色正是他要的。
> 被推翻的只是我设计的**机制**，不是产品需求。
>
> **结论四（新信息）：codex 和 opencode 会发 DECSCUSR（光标形状）**，claude / pi / grok 不发。
> 所以 `PSEUDOCONSOLE_PASSTHROUGH_MODE` 的取舍**只对这两个 agent 有实质意义** ——
> D-9 反转触发条件的验证重点应放在它们身上。

### D-4 · 不建索引数据库
全量扫描 483ms，重建成本低于维护索引的复杂度。**每次启动重扫。**

### D-5 · 数据模型：三个正交概念
这是整个设计的地基。**`active` ≠ `running`。**

| 概念 | 含义 | 由谁改变 | 是否持久化 |
|---|---|---|---|
| **工作集成员**（active） | 「这个会话在我的工作台上」 | **只由用户**：New session 进入 / End session 退出 | **是** |
| **运行状态**（running） | 有没有挂着活 PTY | 进程生死、重启 | 否，运行时属性 |
| **可见性**（tab） | 有没有被调到顶部 tab | 用户点击 | 是（记住上次布局） |

**这个拆分让「重启恢复」变成一件小事**：重启后工作集完好无损，只是全部处于「未跑」状态；
恢复 = 把状态从「未跑」变回「在跑」，**不是重建列表**。

### D-6 · 存储：两个 JSON 文件，不用数据库

`%APPDATA%\<app>\workspace.json` —— 工作集（几十条）

```jsonc
{
  "version": 1,
  "sessions": [{
    "agent": "claude",
    "sessionId": "23497925-1fbb-...",   // 与 agent 一起构成主键；已验证稳定
    "cwd": "C:\\Users\\PC\\Desktop\\Recent\\multi_agents_desktop_app",
    "addedAt": "2026-08-12T07:00:00Z",
    "note": null,        // 用户手写备注，覆盖自动摘要
    "openInTab": true    // 上次是否在顶部 tab
  }]
}
```

`%APPDATA%\<app>\summaries.json` —— 摘要缓存（847+ 条，约 200KB）

```jsonc
{
  "claude:23497925-1fbb-...": {
    "text": "调研竞品并重新定位 MVP 范围",
    "sourceMtime": 1754985000,   // 源文件 mtime，变了才重新生成
    "model": "deepseek-chat"
  }
}
```

### D-7 · 摘要：两层，输入 = 首条 + 过滤后的尾部，不做「实时」

**分层**
- **第 0 层（免费 / 即时 / 离线）**：首条用户消息截断，**列表永不阻塞在网络请求上**。
- **第 1 层（DeepSeek，异步覆盖）**：后台压缩成一句话，回来了替换第 0 层。

**输入范围 = 首条 prompt + 尾部 N 条消息**（不是只发首条）。
理由：会话会漂移 —— 尾部在做的事经常和首条 prompt 完全无关，只发首条只能说明它**出发时**要干什么。

**但尾部必须先过滤**（依据 §2.4 实测）：
1. 从文件末尾 **seek 反向读**，绝不整文件读（存在 195 MB 的单会话文件）
2. **丢弃 `tool_result` / `tool_use` 行**（占尾部 56%），只留纯文本消息
3. 每条截断到 ~500 字符，**整体字节封顶 ~4 KB**

这一步同时压掉成本（63.6 KB → 4 KB）、避免上下文爆掉、并把源码挡在外面（源码在 `tool_result` 里）。

**其它**
- **OpenCode 跳过第 1 层** —— 它自带 `title` 且质量很好。适配器接口出一个 `nativeTitle?: string`，有就直接用。
- **不做实时**：触发时机是「源文件变了 + 静默 30 秒」。开在 tab 里的会话你看得见，不需要摘要；摘要的价值全在**你看不见的那些**。

### D-8 · 默认从 0 开始；扫描和摘要都是 opt-in

用户装这个应用之前，机器上**已经有**别的 agent 的历史会话。**默认一条都不碰。**

| 开关 | 性质 | 默认 |
|---|---|---|
| **扫描历史会话** | 纯本地读盘，零外发 | **关** |
| **生成摘要**（发 DeepSeek） | **内容出境** | **关** |

- 两个开关**独立** —— 可以只扫描不总结（那就只有第 0 层的截断摘要，全程离线）
- 扫描可**按粒度选择**：整个 agent / 某个文件夹下的会话
- 首次打开应用 = **空工作台**，从 New session 开始。想找回历史的，走显式的导入流程

**代价（明确接受）**：首次打开没有「哇，847 个会话」的效果。换来的是用户对自己数据的完全控制，
以及一个不会在用户不知情时把源码发出去的产品。

### D-9 · 技术栈：Electron + xterm.js + node-pty

适配器和主进程一律 **TypeScript**。

**理由**
1. **D-2 是生命线** —— node-pty 是微软为 VS Code 维护的，Windows ConPTY 的边角有几百万人每天在跑。
   选 Tauri 意味着盯着 `portable-pty-psmux` 这个 fork，**那是 own 更多，与 D-2 相反**。
2. 五个适配器是 TDD 最重的部分，TS 人体工学更好；性能完全不是理由（PowerShell 扫 847 个会话只要 483ms）。
3. **VS Build Tools 2022 已装**，A 唯一的已知痛点（Windows 原生模块构建）前提已满足。

**明确接受的代价**
- 包体 ~150 MB（vs Tauri ~8 MB）
- **放弃 `PSEUDOCONSOLE_PASSTHROUGH_MODE`** —— ConPTY 会经自己的屏幕缓冲重绘一遍，
  理论上 TUI 保真度略低于直通。但 VS Code 就是这么跑的，说明够用。

**✅ 反转触发条件已验证：未命中。技术栈维持 Electron + node-pty。**
（2026-08-12，完整报告见 `openspec/changes/prototype-terminal-fidelity/findings.md`）

- 四个 agent（claude / codex / pi / grok）的 passthrough 开/关 A/B：
  **没有任何一类转义序列只在一侧出现** —— ConPTY 不开 passthrough 没有吞掉或改写任何序列，
  包括 DECSCUSR（光标形状）和备用屏幕缓冲切换
- 计数差异全部落在**同参数重复实验的抖动区间**内（codex 全部命中；grok 的动画 TUI
  同参数三次就在 92k–100k 字节之间摆动）
- **反向证据**：`portable-pty` 的出厂标志（`INHERIT_CURSOR|RESIZE_QUIRK|WIN32_INPUT_MODE`）
  在这些 agent 上**是坏的** —— `WIN32_INPUT_MODE` 让 agent 的 `ESC[6n` 查询无人应答而卡死，
  只收到 4 字节。改用 Tauri 反而要打补丁
- **方法论修正**：原计划「node-pty vs portable-pty」是错的 ——
  node-pty 传标志 **0**（`conpty.cc:292`），portable-pty 传 **7**（硬编码），
  差三个标志去比 passthrough 等于同时动四个变量。
  实际做法是 vendor 并补丁 `portable-pty`，用同一个二进制翻转标志组，**只差一个 bit**

**尚存缺口**：opencode 的 passthrough 臂缺失（它在 Rust 探针下不稳定，但 `flags=0` 同样失败，
故与 passthrough 无关）；字节级无差异不等于渲染级无差异，最终视觉判定仍需任务 6.x。

### D-10 · MVP 五个 agent 一次做完

**风险排序已经变了。** README 说「适配器是风险最高的部分」——
实测之后不成立：847 个会话 483ms 扫完、Claude cwd 命中 486/487、OpenCode 一条 SQL 全拿到、
§2.6 五种 transcript 结构全部摸清。**适配器现在是本项目风险最低的部分。**

真正的风险已转移到：
1. **ConPTY 下的 TUI 保真**（D-9 的反转触发条件）
2. **工作集 / explorer / tab 这套 UI 模型到底好不好用**

五个适配器互不依赖，是并行任务（用全局 `dispatching-parallel-agents`）。

### D-11 · 先不建 `AgentAdapter` 接口

README §4.1 在写任何实现之前就把接口设计好了 —— 那是**投机性抽象**，
而且现在建，它只会长成 Claude 的形状（比如「一个会话 = 一个文件」这个假设，
在 Grok 身上直接崩：**Grok 的一个会话是一个目录**）。

**做法**：先写五个具体函数，**等实现都在了再抽接口**（rule of three）。
接口的形状由已知实现决定，不是反过来。这条是 `ponytail-review` 的重点检查项。

### D-12 · 批量恢复：串行、后台、带实时进度

点「恢复选中」后**一次只起一个**，起完再起下一个，全程后台跑，顶部显示「正在恢复 3/6」，
期间用户可以正常操作已经起来的会话。

**理由**：本机负载是 8 个 claude 会话 × 约 15 个 MCP 子进程 = **破百**（README §10）。
6 个并行恢复 = 瞬间 90+ 进程 + 6 份上下文重载的 token 费用。

**默认不自动执行** —— 这条**按精神实现，字面做法在实现期被否决**（`add-workspace`）。

原本的字面做法是「把 `claude --resume <id>` 打进终端但不回车」。它做不了，也不该做：

1. **技术上不成立。** agent 是在 PTY 里跑的 TUI，不是等命令的 shell。
   「打进终端不回车」需要先有一个 shell 会话，等于多起一层进程，
   而那层 shell 一旦存在，`resume` 的进程树、信号、退出码就全变了样。
2. **它把确认点放错了地方。** 需要用户确认的是「要不要花这份 token」，
   不是「要不要按回车」。

**实际做法**：启动时弹恢复面板，成员默认勾选但**要按一次「恢复选中」**；
面板里也可以「稍后再说」，那些成员就以「未启动」状态留在侧栏，
每行**显示将要执行的完整命令**（如 `claude --resume 85a3eab9…`），点一下才真起。
确认点还在，而且比原方案更明确 —— 用户看到的是命令本身，不是一个待回车的终端行。

串行、后台、实时进度这三条**原样保留**，`restoreSerially` 有 7 个测试守着，
其中「串行」是用**并发计数峰值 `maxInFlight === 1`** 断言的，不是只看调用顺序 ——
顺序对但重叠了照样会打满机器。

**不跨应用重启续做** —— 已恢复的会话是本应用的子进程，应用关闭它们就没了，续做没有意义。

### D-13 · 扫描 + 摘要是可续的后台作业，但不建作业队列

D-8 的 opt-in 导入才是真正的长任务：847 个会话 + 约 678 次 DeepSeek 调用，分钟级，
**必须能跨应用重启续做**（关掉再打开，接着做，带实时进度）。

**用「结果本身」表示进度，不建 job queue：**

- 续做 = 「列出目标范围内**还没有缓存摘要**的会话，接着做」
- 进度 = `已缓存数 / 目标总数`
- 天然幂等、天然可续、崩溃安全、零额外状态

同 D-11 的精神：不为「作业管理」建抽象，用已有的数据结构（`summaries.json`）表达状态。

### D-14 · 主题系统：内置 5 套，默认 GRAPHITE；自定义走文件不走 UI

**主题在本架构里是数据不是代码** —— 应用 1 套和应用 5 套是同一条代码路径
（一个对象 → xterm.js 的 `theme` + 一组 CSS 变量）。多出来的只有一个下拉框和 5 份数据。

**两个正交的选择**（同 D-5 的思路，不做成 10 个平铺项）：

| 选择 | 取值 |
|---|---|
| **主题**（身份） | GRAPHITE · HARBOR · EMBER · PAPER · PHOSPHOR |
| **明暗**（环境） | 跟随系统 / 强制浅色 / 强制深色 |

**默认 GRAPHITE**：底色中性，不给 agent 输出强加色相（五个 agent 各有品牌色）；
对比度最高，报错的红最跳；浅色变体最稳。D-3 说主题是「地面」，地面就该安静。

**自定义 = 配置目录里的一个 JSON 文件，不做取色器 UI。**
想改就改、想加就丢新文件进去。零 UI 成本、完全自由度，顺带成了开源的贡献路径
（别人可以直接贴 Tokyo Night / Catppuccin 进来）。
19 个色槽的取色器界面是 `ponytail` 要砍的「没人要求的灵活性」——
先用文件顶着，等真有人抱怨再做。

**PAPER 是浅色优先**的方案（ANSI 为浅底调过，不是把深色翻过来）；
**PHOSPHOR 天生是深色**方案，浅色变体属于兼容性提供。

> 这两句是给人看的设计说明，**不进主题文件格式**。
> 曾经有过一个 `prefers` 字段想把它编码进去，但它被校验、被测试、被写进 5 个 JSON，
> 却没有任何一行代码读它 —— 一个"有测试保护的注释"。
> 已于 2026-08-13 的 `ponytail-review` 中删除。
> 将来真要做主题选择器并显示"浅色优先"标签时再加回，成本约十分钟。

---

### D-W1 · 工作集条目只存 4 个字段，不缓存索引信息

`{agent, sessionId, cwd, addedAt}`。**不存** `lastActivity` / `nativeTitle` / `cwdExists`。

**理由**：工作集的职责是「记住你在用哪些」，不是「缓存索引」。缓存的索引字段会过期，
而过期的信息比没有信息更糟 —— 界面会理直气壮地显示错的东西。要新鲜信息就去扫（D-4：约 400 ms）。

「工作目录还在不在」因此是**读取时现算**，不落盘。

这三个字段正好是 `resumeCommand` 需要的全部 —— 不是巧合，是这条约束的目的。

### D-W2 · 成员资格与运行状态正交，靠**结构**保证而不是靠纪律

D-5 说「进程退出不改变成员资格」。实现上不是靠约定「记得别改」，
而是 `WorkspaceEntry` **结构上就没有运行状态字段**。没有可改的东西，就不可能改错。
测试断言 `Object.keys` 恰好是那四个。

### D-W3 · 新建的会话进工作集时 `sessionId` 是 `null`

实现期才发现的事实：**spawn 那一刻拿不到会话 id** —— id 是 agent 自己生成并写进
它自己的会话文件的，应用无从得知。

所以 `sessionId` 是 `string | null`，null 的条目靠「继续该目录下最近的会话」恢复：

| agent | 有 id | 无 id（继续最近的） |
|---|---|---|
| claude | `--resume <id>` | `-c` |
| codex | `resume <id>` | `resume --last` |
| opencode | `--session <id>` | `-c` |
| pi | `--session <id>` | `-c` |
| grok | `--resume <id>` | `-c` |

**连带的主键缺陷（实现期自己发现并修掉）**：主键若只看 `(agent, sessionId)`，
两个 id 都为 null 的条目会被误判成重复 —— 同一个 agent 在两个目录各新建一个，
第二个会被**悄悄丢掉**。主键改为 `(agent, sessionId ?? cwd)`。
这类 bug 不报错，只让会话神秘消失。

### D-W4 · 主键必须能安全地穿过 DOM 属性

主键会被写进 `data-key` / `data-end`，再读回来做查找。分隔符最初用 `U+0000`，
**HTML 属性解析会把它改写成 `U+FFFD`**（Electron 43 实测：写进去 charCode 0，读回来 65533）。

后果是每一次按 key 的查找都失配，而且**一声不响**：结束会话、点未启动的成员启动、
切标签页、恢复面板单行勾选 —— 四个交互全成了哑的。分隔符改用 `|`（Windows 路径里非法）。

同一类坑的第二个入口：Windows 路径里 `&` 是**合法字符**，
`data-key="a&amp;b"` 不转义就会读回成 `a&b`。所有插进 HTML 的用户字符串都要转义，
**属性值也要**。

> 记在这里是因为它是一类问题而不是一个 bug：**任何「把标识符塞进 DOM 再读回来」的地方，
> 都要先确认这个标识符能原样往返。**

### D-W5 · 工作集每次变化立即落盘，不做防抖

防抖会引入「崩溃时丢掉最后一次变化」的窗口，而这个应用的**全部价值**就是「关机后会话不丢」。
写的是几百字节的 JSON，省这点 IO 换一个能让核心承诺失效的窗口，不划算。

### D-W6 · 「结束会话」= 杀进程 + 移出工作集（Q7 定案）

一个动作做两件事，理由是**用户的意图只有一个**：「我不再用这个会话了」。
拆成两个动作（「关掉但留着」「移出工作集」）会逼用户去理解应用内部的两个概念。

历史会话本身不受影响 —— agent 自己的会话文件还在，随时能从「历史会话」里再恢复回来。
所以这个动作是可逆的，不需要二次确认。

**验证方式是查整棵进程树**：记下 PTY 的 pid，展开全部子孙，点 ✕ 之后逐个核对是否消失。
只看根 pid 会漏掉 agent 起的一堆子进程 —— 实测一个 claude 会话是 **27 个进程**。
检查必须在应用**还活着**的时候做：等退出后再看，`will-quit` 的兜底清理会把漏杀的一起收走，
那就永远测不出「结束会话」自己漏没漏。

### D-F1 · 会话行第二行是一条优先级链

四处（工作集 / 历史 / 收藏 / 恢复面板）统一显示：

```
摘要（D-7，还没做） ?? agent 自带标题（只有 opencode 有） ?? 开头那句用户消息
```

**呈现形式定为「摘要就是标题，占行内第二行」**，不藏在问号 tooltip 后面。
理由：摘要是用户唯一能读懂的那行字，它是**主要**信息；把主要信息当辅助信息藏起来，
还让列表在悬停之前无法扫视 —— 找会话会退化成逐行悬停。

**为什么不统一用首条消息**：opencode 自带的 title 是模型生成的真标题，质量高于原始首条消息。
用更差的盖掉更好的，是为了「一致」牺牲用户能读到的信息。
（grok 也有自带摘要，但它是粗暴截断，比我们自己截还差，不进这条链 ——「有」不等于「可用」。）

**身份与意义是两回事**：行内另有一个暗色的 6 位 session id。它用来确认「是哪一个」，
不是用来读的。所以不去发明第三套命名。

### D-F2 · 注入过滤必须是结构规则，不能是白名单

`§2.6` 那张注入表是白名单。本变更在真实数据里**连着撞见三个表上没有的包装**：

| 发现 | 形态 |
|---|---|
| grok | `<user_query>…</user_query>` 把用户真话包在里面 |
| codex | `<recommended_plugins>` —— 以**用户消息**身份发的插件推荐 |
| （表里已有） | `<system-reminder>` / `<user_info>` / `<command-*>` |

白名单永远列不全，而漏掉的代价是用户在界面上看见一段系统提示。所以改成**结构规则**：

- **以 XML 式开标签起头的一律当注入**（`<user_query>` 例外，剥开取里面的话）
- 测试里同时断言「预览不以 `<` 开头」—— 下一个未知包装会自己暴露，
  而不是等用户看见。**codex 那条就是被这条断言抓出来的。**

「有信息量」的判定（可测，不是感觉）：剥包装 → 跳斜杠命令 → 压缩空白 →
仍短于 20 字则看下一条 → 截断到 46 字 → 全都不行返回 `null`，
界面显示「（没有可读的开头）」而**不是空白**（空白看起来像还在加载）。

**实测提取率 392/433 = 90.5%**（claude 93% / codex 86% / opencode 89% / pi 67% / grok 96%）。

> 已知取舍：长度只是信息量的**代理指标**，会误杀真正有信息的短句。
> 不再加一层启发式去追这个代理 —— 真正的解法是摘要，不是更聪明的阈值。

### D-F2b · 头部读取要有**字节**上限，不能只有行数上限

`readHead` 原本只按行数停。实测 pi 的会话头部有把整个 README 塞进去的 toolResult 行，
40 行能有好几 MB —— 取一条预览要 **18.8 ms**，其他 agent 是 0.5–3.3 ms。

加 256 KB 上限后：claude 232→59 ms，pi 226→7 ms。整轮扫描 **336 → 665 ms**（433 条），
在「超过 1 秒就退回惰性方案」这条线之内，所以预览接在扫描里，不做惰性。

### D-F3 · 收藏条目的 `sessionId` 不可为空

与 `WorkspaceEntry` 的关键差别。工作集条目可以是「该目录下最近的会话」（D-W3），
而**收藏一定指向一个具体会话** —— 「以后还要用这个」指的是那一个，不是那个目录里最新的那个。

主键因此是 `(agent, sessionId)`，不需要工作集那套 `?? cwd` 兜底。

代价如实记下：**在 agentory 里新建的会话收藏不了**（没有 id 可指）。

### D-F4 · `label` 存快照，且这不违反 D-W1

D-W1 禁的是**会过期**的字段（`lastActivity` / `cwdExists`）。
首条用户消息是 append-only 历史里**不可变**的东西 —— 它永远不会变。

存下来换到的是**启动时零文件读**：侧栏收藏区块要在启动时就渲染，而扫描是 opt-in 的
（D-8，默认关）。不存快照就只能现读文件或强制扫描，两者都更糟。

实测的副产品：**工作目录被删之后，收藏行仍然显示得出它是什么** ——
快照活得比它描述的东西久。

### D-F5 · 落盘复用同一层外壳，不复制校验

`favorites.json` 与 `workspace.json` 同形状（`{version:1, sessions:[…]}`，带缩进）。
文件外壳抽成 `loadEntryFile(path, parseOne)`：读 → parse → 逐条校验（**单条坏只跳过那条**）
→ **失败绝不写回**。

抽这一层不是为了"复用"这个词，而是让「绝不替用户销毁记录」这条已有的测试
**同时守住两个文件**。复制一份校验 = 那条测试只守住其中一个。

### D-F6 · 星标与收藏项都必须有冒烟守门

两者都靠 `data-key` 查找，正是上一刀里被主键中的 U+0000 悄悄哑掉的那类路径（D-W4）——
那次四个交互全是死的却一个错都没报。所以本刀新增的两条同类路径各配一条自动点击检查。

### D-U1 · 界面细节的定案（第一次真正「看」过之后）

在此之前从没截过图，只读 CSS 想象渲染结果。第一次截图就抓到三处：

| 发现 | 性质 |
|---|---|
| **`.btn-search` 在整份样式表里不存在** | 「历史会话」按钮退化成系统原生按钮 —— 深色主题里一个浅色小方块，文字被挤掉一半。它从 `add-app-shell` 起就是坏的 |
| **滚动条是系统默认样式** | 深色界面里一条浅灰。五套主题换过去它纹丝不动 —— 主题漏了一个洞 |
| **终端区没有空态** | 一个会话都没起时是一整片黑 |

定下的三条：

1. **主区域必须有空态。** 空白不是「干净」，是「不知道该干什么」。
2. **滚动条属于主题。** 任何不跟着 `--c-*` 走的像素都是主题系统的漏洞。
3. **不放假的交互提示。** 分组头上原本有个 `▾`，点它什么也不会发生 ——
   已删。同理，「历史会话」右边原本挂着一个 `—`（扫描是 opt-in，数字要打开才知道），
   读起来像 bug，改成不知道就不显示。

侧栏纵向节奏也一并改了：工作集与收藏**都靠上**，底栏用 `margin-top:auto` 推到底 ——
原来靠 `.tree` 撑满，两条会话时收藏区孤零零漂在屏幕底部。

收藏行里的 ★ **不承担状态**（收藏区里每条本来就是收藏），它只是移出按钮，
所以默认压暗、悬停才亮，与工作集那个 ✕ 同一套语言。

**浅色主题一并验过**（PAPER + light），不是只在深色里好看。

### D-L1 · 应用图标：卡带（提案 2C，配色 F 青→紫）

**形**：一盘能插进去、拔出来、留着不丢的卡带 —— 「关机也不丢」的实体。

**为什么不是字形**：第一轮做了六个描边字形，全部作废。它们是照 Lucide 图标规格
（24 网格、2.2 等宽描边、圆角端点）画的**工具栏图标**，放进任务栏就是「又一个开发工具」。
换成 64 网格 + 填充笔画 + 渐变 + 投影之后才开始像 logo。

**颜色的教训比色号本身重要**。第一版用赭橙（`#BE4E22`），用户的反馈是「不像科技 app」。
诊断出来是三件事，只有一件跟色号有关：

1. 赭橙是**陶土色**，不是发光色，缩小后发褐
2. 外壳是暖灰 → 整体读成陶器
3. 辉光弥散 → 像烛火不像屏幕

改的是**材质叙事**：冷近黑外壳 + 干脆的顶边高光 + **金色触点** + 高彩度自发光屏。

> **金色触点是提升最大的一处，比换任何屏幕色都管用** —— 因为它引入了**第二种材质**。
> 只有一种材质的图标读起来是「图形」，两种材质开始读起来像「东西」。

### D-L2 · 三条不能动的图标约束

1. **背景必须透明。** 任务栏 / 开始菜单 / 桌面快捷方式都是把图标合成到各种底色上，
   烤一个深色方块进去就是一块突兀的瓷砖。
2. **外壳必须有亮边（rim light）。** 外壳是近黑，深色主题的任务栏也接近黑 ——
   没有那道亮边，外壳会整个消失，只剩一块发光的屏悬在那里。
   已在 `#000000`…`#333333` 与 `#FFFFFF`…`#DDDDDD` 两组底色下逐档验过。
3. **辉光要 clip 在外壳内。** 漏到外面会在透明底上留一圈毛边。

### D-L3 · 16/20px 另画一版，不是把大图缩小

大图在那个尺寸会糊：三行文字挤成一坨、四个触点连成一条、辉光吃掉边缘。
简化版把文字压成两行并加粗、触点减到三个并加宽、去掉辉光与内嵌阴影。
生成器按尺寸自动选源（`≤20px` 用 `icon-small.svg`）。

### D-L4 · 图标资产零依赖生成

`scripts/make-icons.cjs`：Electron 把 SVG 画进 canvas → `toDataURL('image/png')` → 组装 ICO。

- 刻意**不用 `capturePage`** —— 那条路的 alpha 取决于窗口透明度设置，不可靠；
  canvas 的 alpha 是确定的，而且能当场把角落像素读出来验
- ICO 只是个容器（6 字节头 + 每图 16 字节目录项 + 若干 PNG 块），
  Windows Vista 起就认 PNG-in-ICO，不需要 BMP 编码器
- 生成后用 `nativeImage.createFromPath` **回读验证**，而不是相信自己刚写的字节

**应用内那个 15px 标记不用这套渐变**：同一个形状，填 `--c-accent`，跟主题走
（渐变在 15px 上本来就退化成一个中间色；而固定的青紫放进 PHOSPHOR 会像贴上去的）。
单色下靠**两档不透明度**（屏 100% / 壳 55%）保住「壳 + 屏」的层次。

### D-U2 · 快捷键全部走 `Ctrl+Shift`，不占终端的键

主区域是终端。`Ctrl+W`（删词）、`Ctrl+A`/`Ctrl+E`（行首尾）、`Ctrl+P`/`Ctrl+N`（历史）、
`Ctrl+C`（中断）在终端里全都有含义 —— 占用它们是实打实的功能倒退。

终端应用从不使用 `Ctrl+Shift` 组合（那是 Windows Terminal 给应用级动作留的命名空间），
所以这个选择**让冲突问题根本不存在**，也就不需要维护一份「哪些键不转发给终端」的例外名单。
那种名单是 VS Code 至今还在维护的东西，也是 bug 的温床。

两个实现上的坑，都有测试守着：

1. **用 `code` 不用 `key`。** `Ctrl+Shift+,` 的 `key` 在多数布局上是 `<` 而不是 `,`，
   按 `key` 匹配会在非美式键盘上悄悄失效
2. **xterm.js 用一个隐藏 textarea 收键盘输入。** 「activeElement 是 textarea 就禁用快捷键」
   这条朴素规则会把终端里的快捷键**全部**禁掉 —— 而那正是最需要它们的地方

F1 面板里除了我们的键，还列出 `Ctrl+C` / `Ctrl+W` 并注明「原样交给终端」——
说明我们**没有**占用它们，和说明我们占用了什么一样重要。

### D-U3 · 铃声用 xterm.js 的 `onBell`，不自己在字节流里找 ``

agent 需要你时会敲终端铃。这是终端协议自带的信号，**不需要解析输出**，跨五个 agent 通用。

**但绝不能自己在字节流里找 ``**：OSC 设置窗口标题的序列本身就以 BEL 结尾
（`ESC ] 0 ; 标题 BEL`），agent 每改一次标题就带一个 —— 朴素检测会疯狂误报。
xterm.js 已经在逐字节解析这个流，它分得清。**我一度写完了自己的检测模块才想起来查，
查完就把它删了。**

策略：应用内标记（侧栏与标签页的圆点 + 状态文字变「需要你」）**常开**；
系统通知**只在窗口失焦时**弹 —— 你正看着应用时再弹一条是噪音。焦点由主进程判断。

**已知前提**：Claude Code 的终端铃不是默认开的，`preferredNotifChannel` 默认 `auto`，
要敲铃得设成 `terminal_bell`。**我们不去改用户的 agent 配置**，只在应用里说明怎么开。

### D-U4 · 恢复从阻塞弹窗改成侧栏横幅

`add-workspace` 之后侧栏已经把「未启动」的成员连同**完整命令**列了出来，点一行就起。
那个启动时的弹窗因此是**冗余**的 —— 它多出来的只有勾选框，而那件事侧栏做得更好
（能看到命令、能看到第二行摘要）。

D-12 的确认点没有丢：**不点就不花 token**。变的只是它不再每次开应用都挡在你面前。

### D-U5 · 设计系统：圆角五档、字号六档

此前圆角有 **10 个**不同的值、字号有 **12 个**（其中 11 / 11.5 / 12 / 12.5 同时在用 ——
肉眼分不出，却是四个各自独立的决定）。全部收进 CSS 变量。

同时补上了**按下态**（此前 `:active` 在整份样式表里出现 **0 次**）和**状态过渡**
（此前 `transition` 只出现 1 次，而那一次是 `prefers-reduced-motion` 的关闭开关 ——
写了兜底，却没有任何动效可关）。

### D-U6 · 历史列表不做虚拟滚动（已量）

`add-session-resume` 遗留的 4.5 终于有数字：**435 行，搜索框每次按键整表重建耗时
0.4 – 8.8 ms**，远在 16 ms 帧预算内。所以什么都不做。

### D-U7 · 图标在未打包时看不出效果，这是 Windows 的行为

`setAppUserModelId` + `BrowserWindow({icon})` 都已接上，图标路径在运行时解析正确。
但**未打包的 Electron 应用，任务栏按钮的图标取自 `electron.exe` 本身** ——
要等 electron-builder 把图标嵌进自己的 exe 才可见。

真机截了任务栏确认过：现在仍是 Electron 默认图标。**这条只能说「接线已验证」，
不能说「效果已验证」** —— 后者要等打包那一步。

### D-U8 · 传给子进程的环境必须清洗（Q10 定案）

**不清洗的话，agentory 会亲手销毁它赖以存在的数据。**

实测：从一个 Claude Code 会话里启动 agentory 时，`process.env` 里带着
`CLAUDE_CODE_CHILD_SESSION`；原样传给子进程后，**新起的 claude 会关掉 transcript 保存** ——
而那正是这个产品要索引的全部内容。

**用剔除名单而不是白名单。** 白名单看起来更安全，但在 Windows 上漏一个就可能让 agent
起不来或行为异常（`HTTP_PROXY`、`LANG`、`NODE_OPTIONS`、公司的证书路径…），
而那类 bug 极难诊断。我们要挡的是一类明确的东西：agent 私有的会话级 / 运行时标记。

**只剔运行时标记，保留看起来像用户配置的。** 分不出「用户自己设的」和「父进程注入的」
是这道题唯一的难点，所以按名字的形状判断：带 `SESSION` / `ENTRYPOINT` / `EXECPATH` /
`PID` / `EFFORT` 的是运行时；`CLAUDE_CODE_DISABLE_AUTO_MEMORY` 这种是配置，保留 ——
剔错了会让用户在系统里设的配置在这个应用里悄悄失效。

> **做真机验证时才发现 `AI_AGENT`。** 它的值直接写着父 agent 的身份与版本
> （`claude-code_2-1-220_agent`），是同一类污染，但名字上完全看不出属于哪个 agent，
> 前缀规则挡不住。**名单还会继续长** —— 这是剔除名单方案的已知代价。

验证方式：起一个真进程把它看到的环境打出来。断言子进程看不到注入标记、
`PATH` 还在、用户配置也还在。该测试会自检前提（不是从 agent 会话里跑的就跳过），
而不是假装通过。

### D-U9 · 本轮 logo + UI 没走 openspec

前 7 刀都走了 openspec（proposal / design / tasks）。**logo + 图标 + UI 细节这一轮是照
plan 文档做的**，没建 openspec 变更。

如实记在这里而不是补一份事后的规格 —— 补出来的规格是假的，它描述的不是当时的决策过程。
决策本身都记在 D-L1…D-L4、D-U1…D-U8 里，那部分没有丢。

## 4. UI 形态

```
┌─ EXPLORER ───────────────────┐┌ TAB: ⬛claude·multi_agents ┬ ⬛pi·ios_app ┬ + ┐
│                              ││                                              │
│ ▾ multi_agents_desktop_app   ││                                              │
│    ● claude   等你输入 · 2m  ││   原样的 Claude Code TUI                     │
│      调研竞品并重定位 MVP     ││   （一个像素都没改，只换了背景和调色板）        │
│    ○ pi       已停 · 昨天    ││                                              │
│      改 PS5.1 的 stderr 坑   ││                                              │
│                              ││                                              │
│ ▾ local_GPU                  ││                                              │
│    ● claude   工作中 · 现在  ││                                              │
│      跑通 vLLM 多卡推理       ││                                              │
│                              ││                                              │
│ ▸ KnowledgeBase         (2)  ││                                              │
│                              ││                                              │
│ ────────────────────────────  ││                                              │
│  🔍 历史会话 (847)            ││                                              │
└──────────────────────────────┘└──────────────────────────────────────────────┘
```

- **左侧 explorer 只放工作集**，按**真实 cwd（项目文件夹）**分组，组内跨 agent 混排
- 每行：agent 标识 · 运行状态 · 最后活动 · **一句话摘要**
- 状态三态：`●` 在跑/工作中 · `◐` 在跑/等你输入 · `○` 已停
- **顶部 tab = 从 explorer「调出来」的子集**。tab 上显示 agent + 文件夹名，**不显示 session id**
- **历史（847 条）不进 explorer**，走独立搜索视图；右键「加入工作区」→ 变成工作集成员

**重启后的恢复条：**

```
┌────────────────────────────────────────────────────────────┐
│ 上次关闭时有 6 个会话在跑。要恢复哪些？                       │
│   ☑ claude · multi_agents_desktop_app · 调研竞品并重定位…    │
│   ☑ claude · local_GPU · 跑通 vLLM 多卡推理                  │
│   ☐ pi     · ios_app_idea · 改 PS5.1 的 stderr 坑            │
│                        [全选]  [恢复选中 (2)]  [稍后]        │
└────────────────────────────────────────────────────────────┘
```

---

## 5. 待定

| # | 问题 | 状态 |
|---|---|---|
| ~~Q1~~ | ~~喂给 DeepSeek 的输入范围~~ | **已定 → D-7（首条 + 过滤后的尾部）** |
| ~~Q2~~ | ~~技术栈~~ | **已定 → D-9（Electron + node-pty，带反转触发条件）** |
| ~~Q3~~ | ~~MVP 覆盖几个 agent~~ | **已定 → D-10（五个一次做完）+ D-11（先不建接口）** |
| ~~Q8~~ | ~~批量恢复的进程风暴~~ | **已定 → D-12（串行后台）+ D-13（可续作业，不建队列）** |
| ~~Q4~~ | ~~resume 是否就地追加~~ | **已实测关闭 → 五个 agent 一致：恢复不新建会话文件，原会话仍在**（claude 70→70 / codex 65→65 / opencode 166→166 / pi 13→13 / grok 120→120）。所以存下来的 sessionId 恢复后依然有效 —— 工作集与收藏夹的整个前提成立 |
| ~~Q5~~ | ~~各 agent 是否发 truecolor~~ | **已关闭 → 见 D-3 的实测修正：五个全发真彩，`COLORTERM` 旋钮不存在** |
| ~~Q6~~ | ~~项目名、license~~ | **已定 → `agentory`，MIT。仓库根 = 本目录，代码进 `src/`** |
| ~~Q7~~ | ~~「End session」是否要同时杀进程~~ | **已定 → 杀进程 + 移出工作集（见 D-W6）** |
| ~~Q10~~ | ~~传给 agent 子进程的环境变量要不要清洗~~ | **已修 → 见 D-U8**。不清洗的话 agentory 会亲手销毁它赖以存在的数据 |
| ~~Q9~~ | ~~pi / opencode 的注入形态~~ | **已实测关闭 → 见 D-F2**：两者都**无注入包装**；但真正的发现是那张表本身靠不住 |

> Q4 / Q5 / Q9 都是**实测题**，不是设计题 —— 交给 TDD 和 week-1 prototype。
> Q6 / Q7 已定，见上。**设计已收敛。**

---

## 6. 明确不做

- 不做 git worktree 隔离 —— 竞品全都在做，但它解决的是「N 个 agent 在同一个 repo 并行」，
  而实际工作流是「N 个 agent 在 N 个不相干的目录」
- 不做 diff 审查 / kanban / 可视化编辑器 —— 那是 Nimbalyst 和 Copilot App 的产品
- 不做 SSH / 远程会话
- 不做终端功能增强（见 D-2）
- **不做主题取色器 UI**（见 D-14）—— 自定义主题走配置目录里的 JSON 文件

---

## 附录 A · 五套内置主题的色值（D-14 的数据）

**文件格式**（每个主题一个 JSON，放配置目录；用户丢新文件进去即新增主题）：

```jsonc
{
  "id": "graphite",
  "name": "GRAPHITE",
  "dark":  { "bg","chrome","line","fg","dim","cursor","accent","sel", "ansi":[16] },
  "light": { ... 同上 ... }
}
```

`ansi` 是标准 xterm 顺序，直接映射到 xterm.js 的 `theme`：
`black, red, green, yellow, blue, magenta, cyan, white,` 然后是对应的 8 个 `bright*`。
`bg / fg / cursor` 同样直接给 xterm.js；`chrome / line / dim / accent / sel` 给应用外壳的 CSS 变量。

```json
[
  { "id":"graphite", "name":"GRAPHITE",
    "dark":  { "bg":"#16181D","chrome":"#1B1E25","line":"#262A33","fg":"#D6D9E0","dim":"#7C838F","cursor":"#7FB0FF","accent":"#7FB0FF","sel":"#232833",
               "ansi":["#2A2E37","#E5646E","#74C98D","#E3B341","#6EA8FE","#C08CE8","#4FC0D0","#C9CDD6","#3D424D","#FF7B85","#8FE0A6","#F5C95A","#8CBCFF","#D4A6F5","#6FD8E8","#F0F2F6"] },
    "light": { "bg":"#FFFFFF","chrome":"#F3F4F7","line":"#E1E3E9","fg":"#21242B","dim":"#6B7280","cursor":"#2563C9","accent":"#2563C9","sel":"#E7ECF6",
               "ansi":["#21242B","#C0303C","#2F7D4E","#9A6B00","#2563C9","#8437B8","#10707F","#6B7280","#454A54","#D9404C","#3A9660","#B57F0A","#3479E0","#9B4FCC","#1A8899","#9AA1AC"] } },

  { "id":"harbor", "name":"HARBOR",
    "dark":  { "bg":"#0E1519","chrome":"#131D23","line":"#1E2A31","fg":"#C3D0D6","dim":"#6E828C","cursor":"#56C3C9","accent":"#56C3C9","sel":"#1A282F",
               "ansi":["#1E2C33","#D9737A","#7FB98A","#D5AC6B","#6BA5C7","#AE8FC4","#56C3C9","#B4C2C9","#2C3E47","#EE8B92","#97D3A2","#EDC583","#85BDDF","#C6A7DC","#6EDBE1","#DDE8ED"] },
    "light": { "bg":"#FAFCFD","chrome":"#E9F1F4","line":"#D4E1E6","fg":"#1B2A31","dim":"#5E7079","cursor":"#0E7C86","accent":"#0E7C86","sel":"#DCE9ED",
               "ansi":["#1B2A31","#B34049","#2E7A48","#8A6212","#24688F","#7A4E9E","#0E7C86","#63767E","#33474F","#CC5560","#3F9159","#A3792A","#3880A8","#9166B5","#1D95A0","#8CA0A8"] } },

  { "id":"ember", "name":"EMBER",
    "dark":  { "bg":"#1A1614","chrome":"#211C19","line":"#2E2622","fg":"#E0D6CC","dim":"#8A7C70","cursor":"#E0894A","accent":"#E0894A","sel":"#2A231E",
               "ansi":["#302823","#D9636B","#A3B565","#E0A84A","#7FA6B8","#C08AA8","#6FB5AC","#C9BEB2","#42372F","#EE7B82","#B8CB77","#F2C063","#96BCCE","#D6A0BE","#85CCC3","#F2EAE0"] },
    "light": { "bg":"#FDFAF6","chrome":"#F2EBE2","line":"#E3D8CB","fg":"#2B231E","dim":"#7A6C61","cursor":"#B35C13","accent":"#B35C13","sel":"#EBE0D2",
               "ansi":["#2B231E","#B03A44","#5A6E24","#8C5A0C","#3A6478","#8A4A6B","#1F6E68","#786A5F","#4A3E36","#C74E58","#6E8430","#A87016","#4C7A90","#A05E81","#2F857E","#A3958A"] } },

  { "id":"paper", "name":"PAPER",
    "light": { "bg":"#FCFAF6","chrome":"#F2EEE5","line":"#E2DCCF","fg":"#26231E","dim":"#746F64","cursor":"#A8541A","accent":"#A8541A","sel":"#EBE5D8",
               "ansi":["#26231E","#A8321F","#3F6B2B","#8A6410","#2B5B96","#7B3B84","#146C72","#6E6A62","#4A463E","#C24632","#52843A","#A67D1F","#3A72B5","#94509D","#21858C","#9C978D"] },
    "dark":  { "bg":"#1D1C19","chrome":"#24231F","line":"#302E29","fg":"#E8E3D8","dim":"#857F73","cursor":"#E0A05A","accent":"#E0A05A","sel":"#2B2924",
               "ansi":["#33312B","#DE7167","#9DC17E","#E3B75E","#7FA9D6","#C08FCB","#62BDC0","#C7C1B5","#454239","#F0887E","#B2D493","#F0CA75","#96BEE5","#D3A6DC","#77D2D5","#EEE9DD"] } },

  { "id":"phosphor", "name":"PHOSPHOR",
    "dark":  { "bg":"#0B0B09","chrome":"#100F0C","line":"#1C1B16","fg":"#D8CBA8","dim":"#7E7659","cursor":"#FFB000","accent":"#FFB000","sel":"#1A1812",
               "ansi":["#1F1E19","#E0574F","#96C246","#FFB000","#6FA8C4","#C08AB0","#58BFB2","#C6BCA0","#34322A","#FF6E63","#ADD95C","#FFC734","#86C0DC","#D6A3C7","#6DD8CA","#EFE6CC"] },
    "light": { "bg":"#F7F2E4","chrome":"#EDE7D5","line":"#DDD5BE","fg":"#2A2519","dim":"#746C55","cursor":"#9A5B00","accent":"#9A5B00","sel":"#E5DCC3",
               "ansi":["#2A2519","#A83A2E","#4E6B1C","#8A5A00","#2D5F7A","#7A3E68","#14675E","#6E6552","#45402F","#C24E3F","#63852A","#A87308","#3B7594","#94537F","#1E837A","#9A9179"] } }
]
```

> **2026-08-13 真机复核完成**：在跑着真实 claude 会话的 Electron + xterm.js 应用里逐套看过，
> 用户判定 **五套全部保留、色值不做修改**。Q5 亦已关闭（见 D-3 的实测修正）。
> 复核关注的三处：报错的红够不够跳、diff 的红绿分不分得开、侧栏摘要的灰够不够读。
